{
  "annotations": {
    "list": []
  },
  "editable": true,
  "gnetId": null,
  "graphTooltip": 1,
  "id": null,
  "links": [],
  "panels": [
    {
      "datasource": "Prometheus",
      "fieldConfig": {
        "defaults": {
          "color": {
            "mode": "thresholds"
          },
          "mappings": [],
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {"color": "red", "value": null},
              {"color": "yellow", "value": 99.5},
              {"color": "green", "value": 99.9}
            ]
          },
          "unit": "percent"
        }
      },
      "gridPos": {"h": 8, "w": 8, "x": 0, "y": 0},
      "id": 1,
      "options": {
        "orientation": "auto",
        "reduceOptions": {"calcs": ["lastNotNull"]},
        "showThresholdLabels": true,
        "showThresholdMarkers": true
      },
      "targets": [
        {
          "expr": "(sum(rate(http_requests_total{status!~\"5..\"}[30d])) / sum(rate(http_requests_total[30d]))) * 100",
          "refId": "A"
        }
      ],
      "title": "Availability SLO (30d)",
      "type": "gauge",
      "description": "Target: 99.9% availability. Shows percentage of successful requests over 30-day rolling window."
    },
    {
      "datasource": "Prometheus",
      "fieldConfig": {
        "defaults": {
          "color": {"mode": "thresholds"},
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {"color": "green", "value": null},
              {"color": "yellow", "value": 50},
              {"color": "orange", "value": 80},
              {"color": "red", "value": 100}
            ]
          },
          "unit": "percent"
        }
      },
      "gridPos": {"h": 8, "w": 8, "x": 8, "y": 0},
      "id": 2,
      "options": {
        "orientation": "auto",
        "reduceOptions": {"calcs": ["lastNotNull"]},
        "showThresholdLabels": true,
        "showThresholdMarkers": true
      },
      "targets": [
        {
          "expr": "((1 - (sum(rate(http_requests_total{status!~\"5..\"}[30d])) / sum(rate(http_requests_total[30d])))) / 0.001) * 100",
          "refId": "A"
        }
      ],
      "title": "Error Budget Consumed (30d)",
      "type": "gauge",
      "description": "Percentage of error budget consumed. Green <50%, Yellow 50-80%, Orange 80-100%, Red >100%"
    },
    {
      "datasource": "Prometheus",
      "fieldConfig": {
        "defaults": {
          "color": {"mode": "thresholds"},
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {"color": "green", "value": null},
              {"color": "red", "value": 0.5}
            ]
          },
          "unit": "s"
        }
      },
      "gridPos": {"h": 8, "w": 8, "x": 16, "y": 0},
      "id": 3,
      "options": {
        "orientation": "auto",
        "reduceOptions": {"calcs": ["lastNotNull"]},
        "showThresholdLabels": true
      },
      "targets": [
        {
          "expr": "histogram_quantile(0.99, sum(rate(http_request_duration_seconds_bucket[30d])) by (le))",
          "refId": "A"
        }
      ],
      "title": "p99 Latency (30d)",
      "type": "gauge",
      "description": "Target: <2s. 99th percentile request latency over 30-day window."
    },
    {
      "datasource": "Prometheus",
      "fieldConfig": {
        "defaults": {
          "color": {"mode": "palette-classic"},
          "custom": {
            "axisPlacement": "auto",
            "drawStyle": "line",
            "fillOpacity": 20,
            "lineWidth": 2
          },
          "unit": "percent"
        }
      },
      "gridPos": {"h": 8, "w": 24, "x": 0, "y": 8},
      "id": 4,
      "options": {
        "legend": {"calcs": ["mean", "min"], "displayMode": "table"}
      },
      "targets": [
        {
          "expr": "(sum(rate(http_requests_total{status!~\"5..\"}[1h])) / sum(rate(http_requests_total[1h]))) * 100",
          "legendFormat": "1h window",
          "refId": "A"
        },
        {
          "expr": "(sum(rate(http_requests_total{status!~\"5..\"}[6h])) / sum(rate(http_requests_total[6h]))) * 100",
          "legendFormat": "6h window",
          "refId": "B"
        },
        {
          "expr": "(sum(rate(http_requests_total{status!~\"5..\"}[30d])) / sum(rate(http_requests_total[30d]))) * 100",
          "legendFormat": "30d window (SLO)",
          "refId": "C"
        }
      ],
      "title": "Availability - Multi-Window Burn Rate",
      "type": "timeseries",
      "description": "Availability across multiple time windows. 1h for fast detection, 6h for sustained issues, 30d for SLO compliance."
    }
  ],
  "refresh": "1m",
  "schemaVersion": 36,
  "style": "dark",
  "tags": ["slo", "error-budget"],
  "templating": {"list": []},
  "time": {"from": "now-7d", "to": "now"},
  "timepicker": {},
  "timezone": "",
  "title": "SLO Dashboard",
  "uid": "slo-dashboard",
  "version": 1
}
